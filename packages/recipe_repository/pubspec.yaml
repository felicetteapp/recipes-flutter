name: recipe_repository
description: Dart package which manages the recipe domain.
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  cloud_firestore: ^6.0.3
  equatable: ^2.0.7
  uuid: ^4.5.2
