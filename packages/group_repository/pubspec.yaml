name: group_repository
description: Dart package which manages the group domain.
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  cloud_firestore: ^6.0.3
  equatable: ^2.0.7
  ingredient_repository:
    path: ../ingredient_repository
  recipe_repository:
    path: ../recipe_repository
